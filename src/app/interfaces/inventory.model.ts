//a model dedicated to defining the structure of the data in the products page

export interface Inventory{
    id: string;
    title: string;
    category: string;
    price: string
    picture: string;
}